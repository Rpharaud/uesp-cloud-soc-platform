# CI Enforcement Policy

The CI pipeline validates:

- Required phase documentation exists
- Markdown formatting passes
- YAML syntax is valid
- No oversized binaries are committed
- Terraform lock consistency maintained
- Required directories exist
- Secret patterns are not detected
- Naming conventions are followed
